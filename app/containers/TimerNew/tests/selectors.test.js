// import { fromJS } from 'immutable';
// import { makeSelectTimerNewDomain } from '../selectors';

// const selector = makeSelectTimerNewDomain();

describe('makeSelectTimerNewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
