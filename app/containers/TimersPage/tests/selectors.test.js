// import { fromJS } from 'immutable';
// import { makeSelectTimersPageDomain } from '../selectors';

// const selector = makeSelectTimersPageDomain();

describe('makeSelectTimersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
